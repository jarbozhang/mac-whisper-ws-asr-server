This repo implements the spec captured in Obsidian:
- ESP32 uploads audio via WebSocket (JSON control frames + binary PCM)
- Mac runs whisper.cpp and returns text

If you need the full spec text, copy it from:
`/Volumes/100.86.103.28/obsidian/20 Areas/Hardware/Claude Code/ESP32 + WebSocket 语音上传 + whisper.cpp ASR（Mac Node）- Spec.md`
