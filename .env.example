HOST=0.0.0.0
PORT=8765
AUTH_TOKEN=change_me

# Client-friendly address (mDNS). Example: "mac-mini.local"
# ESP32 can use this instead of scanning for an IP.
MDNS_HOSTNAME=jiabos-macbook-pro-2.local

# Whisper CLI mode (used if WHISPER_AUTO_START=false and WHISPER_SERVER_URL is not set)
WHISPER_BIN=/ABS/PATH/TO/whisper.cpp/whisper-cli
WHISPER_MODEL=/ABS/PATH/TO/models/ggml-medium.bin
# Example: --language zh --temperature 0
WHISPER_ARGS=--language auto --temperature 0

# Whisper prompt engineering (guides model output style)
# Default: simplified Chinese + programming vocabulary
# Tips: Keep under 224 tokens, use target language style, include domain-specific terms
# WHISPER_PROMPT=以下是普通话的句子。我在写代码，使用 function 定义函数，通过 API 调用接口。

# Whisper server mode (recommended for lower latency)
# Option 1: Auto-start server with Node.js
WHISPER_SERVER_BIN=/ABS/PATH/TO/whisper.cpp/whisper-server
WHISPER_SERVER_HOST=127.0.0.1
WHISPER_SERVER_PORT=8766
WHISPER_AUTO_START=true

# Option 2: Use external whisper server (if already running)
# WHISPER_SERVER_URL=http://localhost:8766

DEFAULT_MODE=paste
MAX_AUDIO_SEC=30
SAMPLE_RATE=16000
CHANNELS=1
BIT_DEPTH=16
KEEP_DEBUG=false
