## Dev notes

- Start server: `npm run dev`
- Health: `GET /health`
- WS: `ws://<host>:<port>/ws`

To use paste/paste_enter on macOS:
- Grant Accessibility permission to your Terminal (or the Node process launcher)

